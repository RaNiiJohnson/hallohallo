# Convex Configuration
# Get these values from https://dashboard.convex.dev after creating a project
CONVEX_DEPLOYMENT=dev:<your-project-name>
NEXT_PUBLIC_CONVEX_URL=https://<your-project-name>.convex.cloud

# Site URL (for Better Auth)
# Use localhost for local development
NEXT_PUBLIC_CONVEX_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_12345
STRIPE_SECRET_KEY=sk_12345

# Stripe Webhook Secret (Get this after setting up webhook endpoint in Stripe Dashboard)
# For local development, use Stripe CLI: stripe listen --forward-to localhost:3000/api/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here